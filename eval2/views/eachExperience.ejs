<!DOCTYPE html>
<html>
<head>
  <title><%= experience.name %></title>
  <link rel="stylesheet" href="/css/godfather.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="../stuff/icon/hotel.svg">
  <style>
    /* Additional styles to complement godfather.css */
    body {
      height: auto !important;
      min-height: 100vh;
      overflow-y: visible !important;
      overflow-x: hidden;
      padding-bottom: 120px;
      position: relative;
    }
    
    .experience-container {
      position: relative;
      padding: 0 200px;
      max-width: 700px;
      margin: 30px auto 100px; /* Added bottom margin */
      z-index: 1;
      background-color: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(2px);
      border-radius: 10px;
      margin-top: 0px;
      margin-bottom: 30px;
    }
    
    h1 {
  font-family: 'Monoton', serif;
  font-weight: 400;
  font-style: normal;
  color: var(--aqua);
  font-size: 3.5rem;
  margin: 2px auto 15px auto;
  text-align: center;
  line-height: 1.1;
  text-shadow: 0 0 12px rgba(20, 255, 236, 0.4);
  position: relative;
  z-index: 2;
}

    
    .experience-image {
      width: 100%;
      max-width: 600px;
      height: auto;
      border-radius: 5px;
      box-shadow: 0 0 7px var(--aqua);
      margin: 20px 0;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      z-index: 2;
    }
    
    .experience-image:hover {
      transform: scale(1.02);
      box-shadow: 0 0 10px var(--aqua);
    }
    
    .detail-item {
      margin-bottom: 15px;
      font-family: 'Source Sans Pro', sans-serif;
      font-size: 1.2rem;
      position: relative;
      z-index: 2;
    }
    
    .detail-label {
      color: var(--aqua);
      font-weight: 600;
    }
    
    .detail-value {
      color: var(--teal);
    }
    
    .price {
      font-family: 'Roboto', sans-serif;
      font-style: italic;
      font-weight: 900;
    }
    
    .amenities-title {
      font-family: 'Modern Antiqua', serif;
      color: var(--aqua);
      font-size: 1.8rem;
      margin: 30px 0 15px;
      position: relative;
      z-index: 2;
    }
    
    .amenities-list {
      list-style: none;
      padding: 0;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      position: relative;
      z-index: 2;
    }
    
    .amenity-item {
      background-color: rgba(13, 115, 119, 0.1);
      color: var(--gray);
      padding: 8px 15px;
      border-radius: 20px;
      border: 1px solid var(--teal);
      font-family: 'Raleway', sans-serif;
      font-weight: 600;
      transition: all 0.3s ease;
    }
    
    .amenity-item:hover {
      background-color: rgba(20, 255, 236, 0.2);
      color: var(--aqua);
      border-color: var(--aqua);
      transform: translateY(-2px);
    }
    
    /* Override the .bot class completely */
    .bot {
      font-family: "Monoton", serif !important;
      font-weight: 400 !important;
      font-style: normal !important;
      font-size: 8rem !important;
      position: fixed !important;
      bottom: 0 !important;
      left: 50% !important;
      transform: translateX(-50%) !important;
      color: var(--gray) !important;
      opacity: 0.3 !important;
      z-index: 0 !important;
      pointer-events: none !important;
      user-select: none !important;
      width: 100% !important;
      text-align: center !important;
      line-height: 0.8 !important;
      white-space: nowrap !important;
      margin: 0 !important;
      padding: 0 !important;
    }
    
    @media (max-width: 768px) {
      body {
        padding-bottom: 80px !important;
      }
      
      .bot {
        font-size: 4rem !important;
        bottom: 10px !important;
        line-height: 0.7 !important;
      }
      
      .experience-container {
        margin: 20px 15px 80px !important;
      }
    }
    
    @media (max-width: 480px) {
      .bot {
        font-size: 3rem !important;
        bottom: 15px !important;
      }
    }

  </style>
</head>
<body>

  <div class="container mt-3">
    <a href="/experience" class="back-link">
        Back
        <img src="/stuff/icon/undo_teal.svg" alt="Back Icon">
    </a>
</div>

  <div class="experience-container">
    <h1><%= experience.name %></h1>
    
    <div style="text-align: center; position: relative; z-index: 2;">
      <img src="<%= experience.image %>" alt="<%= experience.name %>" class="experience-image">
    </div>
    
    <div class="detail-item">
      <span class="detail-label">Price:</span>
      <span class="detail-value price">₹<%= experience.price %></span>
    </div>
    
    <div class="detail-item">
      <span class="detail-label">Rating:</span>
      <span class="detail-value"><%= experience.rating %> / 5</span>
    </div>
    
    <div class="detail-item">
      <span class="detail-label">Description:</span>
      <span class="detail-value"><%= experience.description %></span>
    </div>
    
    <h3 class="amenities-title">Amenities</h3>
    <ul class="amenities-list">
      <% experience.amenities.forEach(item => { %>
        <li class="amenity-item"><%= item %></li>
      <% }) %>
    </ul>
  </div>

  <div class="bot">EXPERIENCE</div>

</body>
</html>
