<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New York Details</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Same styles as hotela.ejs */
        :root {
            --black: #000;
            --dark-gray: #111;
            --gray: #333;
            --teal: #00b8a9;
            --aqua: #00ffdd;
        }

        body {
            background-color: var(--black);
            color: white;
            font-family: 'Raleway', sans-serif;
            overflow-x: hidden;
        }

        .hotel-header {
            background-color: var(--dark-gray);
            color: var(--teal);
            padding: 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 184, 169, 0.3);
            animation: colorPulse 4.2s ease-in-out infinite;
        }

        @keyframes colorPulse {
            0% {
                box-shadow: 0 4px 15px rgba(0, 184, 169, 0.3);
            }
            50% {
                box-shadow: 0 4px 15px rgba(0, 255, 221, 0.5);
            }
            100% {
                box-shadow: 0 4px 15px rgba(0, 184, 169, 0.3);
            }
        }

        .hotel-header h1 {
            font-family: 'Monoton', cursive;
            letter-spacing: -2px;
            animation: colorSwitch 4.2s ease-in-out infinite;
        }

        .hotel-image {
            width: 100%;
            height: 400px;
            object-fit: cover;
            border-radius: 10px;
            border: 2px solid var(--gray);
            transition: transform 0.3s, border 0.3s;
            animation: sick 2s ease-in-out infinite;
        }

        .hotel-image:hover {
            transform: scale(1.02);
            border: 2px solid var(--teal);
            animation: none;
        }

        .hotel-details {
            margin-top: 2rem;
            background-color: rgba(17, 17, 17, 0.7);
            padding: 2rem;
            border-radius: 10px;
            border: 2px solid var(--gray);
            transition: border 0.3s;
        }

        .hotel-details:hover {
            border: 2px solid var(--teal);
        }

        .hotel-title {
            font-size: 2rem;
            font-weight: bold;
            font-family: 'Modern Antiqua', serif;
            color: var(--teal);
            margin-bottom: 1rem;
        }

        .hotel-price {
            font-size: 1.5rem;
            color: var(--aqua);
            font-weight: 600;
        }

        .rating i {
            color: var(--aqua);
        }

        .review-section {
            margin-top: 2rem;
        }

        .review-card {
            background-color: var(--dark-gray);
            padding: 1.5rem;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
            border: 2px solid var(--gray);
            transition: transform 0.3s, border 0.3s;
            animation: sick 2s ease-in-out infinite;
        }

        .review-card:hover {
            transform: translateY(-5px);
            border: 2px solid var(--teal);
            animation: none;
        }

        .review-title {
            font-size: 1.2rem;
            font-weight: bold;
            color: var(--teal);
        }

        .review-content {
            font-size: 1rem;
            margin-top: 0.5rem;
            font-style: italic;
        }

        .footer {
            background-color: var(--dark-gray);
            color: white;
            padding: 2rem;
            text-align: center;
            margin-top: 2rem;
            border-top: 4px solid var(--teal);
        }

        .btn-primary {
            background-color: var(--teal);
            border-color: var(--teal);
            color: var(--black);
            font-weight: bold;
            transition: transform 0.3s, background-color 0.3s;
        }

        .btn-primary:hover {
            background-color: var(--aqua);
            border-color: var(--aqua);
            transform: scale(1.05);
        }

        .btn-warning {
            background-color: var(--aqua);
            border-color: var(--aqua);
            color: var(--black);
            font-weight: bold;
            transition: transform 0.3s, background-color 0.3s;
        }

        .btn-warning:hover {
            background-color: var(--teal);
            border-color: var(--teal);
            transform: scale(1.05);
        }

        .form-control {
            background-color: rgba(51, 51, 51, 0.7);
            color: white;
            border: 1px solid var(--gray);
            border-radius: 8px;
        }

        .form-control:focus {
            background-color: rgba(51, 51, 51, 0.9);
            color: white;
            border-color: var(--aqua);
            box-shadow: 0 0 0 0.25rem rgba(0, 255, 221, 0.25);
        }

        @keyframes colorSwitch {
            0% {
                color: var(--teal);
            }
            50% {
                color: var(--aqua);
            }
            100% {
                color: var(--teal);
            }
        }

        @keyframes sick {
            0% {
                border: 2px solid var(--gray);
            }
            50% {
                border: 2px solid var(--teal);
            }
            100% {
                border: 2px solid var(--gray);
            }
        }
    </style>
</head>
<body>

    <!-- Home Button -->
    <div class="container mt-3">
        <button onclick="navigateHome()" class="btn btn-primary">Home</button>
    </div>

    <!-- Hotel Header -->
    <div class="hotel-header">
        <h1>New York - The City That Never Sleeps</h1>
    </div>

    <!-- Hotel Image -->
    <div class="container">
        <img src="https://images.unsplash.com/photo-1549921296-3a6b8b4b1b9b" class="hotel-image" alt="New York Image">
    </div>

    <!-- Hotel Details -->
    <div class="container hotel-details">
        <div class="row">
            <div class="col-md-6">
                <div class="hotel-title">City Details</div>
                <div class="hotel-price">Explore the vibrant culture and iconic skyline of New York City.</div>
                <div class="rating">
                    <i class="fa fa-star"></i>
                    <i class="fa fa-star"></i>
                    <i class="fa fa-star"></i>
                    <i class="fa fa-star"></i>
                    <i class="fa fa-star"></i>
                </div>
                <p>New York is a global hub for culture, art, and business. From Times Square to Central Park, there's something for everyone.</p>
            </div>
            <div class="col-md-6">
                <div class="hotel-title">Plan Your Visit</div>
                <form id="booking-form">
                    <div class="mb-3">
                        <label for="check-in" class="form-label">Check-in Date:</label>
                        <input type="date" id="check-in" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label for="check-out" class="form-label">Check-out Date:</label>
                        <input type="date" id="check-out" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-warning">Book Now</button>
                </form>
            </div>
        </div>
    </div>
    <div class="container review-section">
        <div class="hotel-title">Customer Reviews</div>
        <div class="review-card">
            <div class="review-title">John Doe</div>
            <div class="rating">
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
            </div>
            <div class="review-content">
                "New York is amazing! The energy of the city is unmatched. Can't wait to visit again."
            </div>
        </div>
        <div class="review-card">
            <div class="review-title">Jane Smith</div>
            <div class="rating">
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
                <i class="fa fa-star"></i>
            </div>
            <div class="review-content">
                "The city that never sleeps truly lives up to its name. So much to see and do!"
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>About New York</h5>
                    <p>New York is a city of dreams, offering endless opportunities and experiences.</p>
                </div>
                <div class="col-md-6">
                    <h5>Contact Us</h5>
                    <p>Email: contact@newyork.com</p>
                    <p>Phone: +123 456 7890</p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.min.js"></script>

    <script>
        // Booking form submission
        document.getElementById('booking-form').addEventListener('submit', function(e) {
            e.preventDefault();
            const checkInDate = document.getElementById('check-in').value;
            const checkOutDate = document.getElementById('check-out').value;

            if (checkInDate && checkOutDate) {
                alert(`Your visit to New York has been planned for check-in on ${checkInDate} and check-out on ${checkOutDate}`);
            } else {
                alert('Please select both check-in and check-out dates');
            }
        });

        // Navigate to home page
        function navigateHome() {
            window.location.assign('/');
        }
    </script>

</body>
</html>